#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
pre_commit=$repo_root/.tools/bin/pre-commit

if [ ! -x "$pre_commit" ]; then
  printf '%s\n' 'pre-commit is not installed; run make setup.' >&2
  exit 1
fi

cd "$repo_root"
exec "$pre_commit" run --hook-stage pre-commit
