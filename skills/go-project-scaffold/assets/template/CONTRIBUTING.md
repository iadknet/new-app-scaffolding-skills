# Contributing

Use `make quick` while iterating and `make check` before opening a pull request.
Keep functions and packages focused; address complexity and duplication findings
instead of suppressing them without a specific reason.
